@model IEnumerable<ApplicationUser>
@{
    ViewData["Title"] = "Index";
}

<h1 class="align-items-center" style="margin-left:40%">User Details</h1>
<a href="/Admin/User/ViewDetails">
    <button style="    width: 203px;
    height: 41px;
    margin-left: 890px" class="btn btn-primary">Booking Details</button>
</a>


@*<div class="d-flex">
    <a asp-action="Create" class="btn btn-success ms-auto my-2">+ Add movie</a>
</div>*@

@*<div class="container-fluid">
    @if (Model.Any())
    {
        <div class="row gap-5">
            @foreach (var item in Model)
            {
                <div class="card col-sm-3 shadow">
                    <img src="https://dummyimage.com/500x500/f2f2f2/5e5e5e&text=" class="card-img-top" alt="">
                    <div class="card-body">
                        <h1>Customer Details</h1>
                        <h6>@item.FirstName</h6>
                        <h6>@item.LastName</h6>
                        <h6>@item.Email</h6>
                    </div>
                    <div class="card-footer d-flex">
                        <div class="btn-group ms-auto">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger" onclick="return confirm('Do you want to delete this movie?')">Delete</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="alert alert-info">
            No movies were found. Please
            <a asp-action="Create" class="alert-link">Add a movie</a> first.
        </div>
    }*@













    <section class="vh-100" style="background-color: white;">
@*        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col col-md-9 col-lg-7 col-xl-5">
                    <div class="card" style="border-radius: 15px;">
                        <div class="card-body p-4">
                            <div class="d-flex text-black">
                                <div class="flex-shrink-0">
@*                                    <img src=""
                                         alt="placeholder image" class="img-fluid"
                                         style="width: 180px; border-radius: 10px;">
                                </div>*@







                                <div class="container-fluid">
                                 @if (Model.Any())
                                    {
                                        <div class="row gap-5">
                                            @foreach (var item in Model)
                                            {


                    <div class="flex-shrink-0">
                        <img src="/images/icon.png"
                     alt="placeholder image" class="img-fluid"
                     style="width: 180px; border-radius: 10px;">
                    </div>



                                                <div class="flex-grow-1 ms-3">
                                                    <h5 class="mb-1">@item.FirstName</h5>
                                                    <h5 class="mb-1">@item.LastName</h5>

                                                    <p class="mb-2 pb-1" style="color: #2b2a2a;">@item.Email</p>
                                                    <div class="d-flex justify-content-start rounded-3 p-2 mb-2"
                                                     style="background-color: #efefef;">

                                                    </div>
                                                    <div class="d-flex pt-1">
                                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>&nbsp;&nbsp;&nbsp;
                                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger" onclick="return confirm('Do you want to remove this user?')">Delete</a>
                                                    </div>
                                                </div>
                                            }
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="alert alert-info">
                                            No users were found.
                                        </div>
                                    }

@*                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>*@
    </section>
